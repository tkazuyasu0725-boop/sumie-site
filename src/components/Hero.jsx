import Scene3D from './Scene3D'
import HeroContent from './HeroContent'

const Hero = () => {
  return (
    <section className="hero">
      <Scene3D />
      <HeroContent />
    </section>
  )
}

export default Hero
